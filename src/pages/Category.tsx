import { useNavigate } from "react-router-dom"; const Category = () => { const navigate = useNavigate(); const collections = ["Accessories","Beauty","Student Essentials","Home & Lifestyle","Shoes & Bags","Nails","Skin Care","Clothing"]; return <main className="page-shell"><button onClick={() => navigate("/")}>? Back to Home</button><h1>Categories</h1><div className="page-image-grid">{collections.map((collection) => <button key={collection} onClick={() => navigate("/categories/" + collection.toLowerCase().replaceAll(" ","-").replaceAll("&","and"))}><div className="collection-image" /><span>{collection}</span></button>)}</div></main> }; export default Category;
