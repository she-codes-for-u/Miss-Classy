import { useNavigate, useParams } from "react-router-dom"; const Collection = () => { const navigate = useNavigate(); const { collection } = useParams(); const name = collection?.replaceAll("-"," "); return <main className="page-shell"><button onClick={() => navigate("/categories")}>? Back to Categories</button><h1>{name}</h1><div className="page-image-grid">{[1,2,3].map((item) => <button key={item}><div className="collection-image" /></button>)}</div></main> }; export default Collection;
